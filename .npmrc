# Package privé @InRealArt sur GitHub Packages
# GITHUB_TOKEN doit être défini (ex. dans .env). Utiliser: pnpm run install:safe
@InRealArt:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

legacy-peer-deps=true
strict-peer-dependencies=false

